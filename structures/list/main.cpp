/*
    compile with debug info:
        g++ -std=c++17 -Wall -g -o main *.cpp
    
    degub memory:
        valgrind --leak-check=yes a.out
    
    execute:
    ./a.out 
    
    */

// List implementation